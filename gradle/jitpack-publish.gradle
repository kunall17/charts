afterEvaluate {
  publishing {
    publications {
      release(MavenPublication) {
        from components.release
        groupId = "com.github.kunall17.charts"
        artifactId = module.getName()
      }
    }
  }
}